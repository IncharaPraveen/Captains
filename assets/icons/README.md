# Extension icons

Add PNG icons here before publishing. Typical sizes are 16, 32, 48, 128, 256,
512, and 1024 pixels. Once added, reference the required sizes in
`manifest.json` under `icons` and `action.default_icon`.
